# {{ name }}

**Aliases**: {{ aliases | map('tagify') | join(', ') }}

**Dominant mana**: {{ mana | map('to_archetype_page_mana') | join(' ') }}

**Type**: {{ type | map('tagify') | join(', ') }}

**Description**: {{ description }}

## **Staples**

{% for staple in staples -%}
[<img src="{{ staple.image_url }}" width="300"/>]({{ staple.page_url }})
{% endfor %}

## **Frequent cards**

{% for frequent in frequents -%}
[<img src="{{ frequent.image_url }}" width="300"/>]({{ frequent.page_url }})
{% endfor %}

## **Decks**

{% for deck in decks -%}
[{{ deck.name }}.{{ deck.owner_name }}]({{ deck.url }}) | *{{ pretty_str(deck.added) }}* | {{ deck.description }}  
{% endfor %}

*Last updated: {{ last_update_date }}.*

